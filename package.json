{
    "name": "iso20022-address-game-js-tests",
    "private": true,
    "version": "0.0.0",
    "description": "Development-only Node/npm tooling for this app's first-party browser JavaScript: unit tests (Vitest + jsdom) and end-to-end browser tests (Playwright + Chromium). Not required to run, build, or deploy the game itself — production JavaScript stays plain, unbundled, and Node-free (see README.md).",
    "license": "GPL-3.0-or-later",
    "type": "module",
    "scripts": {
        "test": "vitest run",
        "test:coverage": "vitest run --coverage",
        "e2e": "./scripts/e2e.sh",
        "e2e:install": "playwright install --with-deps chromium",
        "dast": "./scripts/dast.sh",
        "typecheck": "node scripts/js-typecheck.mjs",
        "typecheck:baseline": "node scripts/js-typecheck.mjs --generate-baseline"
    },
    "engines": {
        "node": ">=20"
    },
    "devDependencies": {
        "@playwright/test": "^1.56.0",
        "@vitest/coverage-v8": "^4.1.11",
        "jsdom": "^30.0.1",
        "typescript": "^7.0.2",
        "vitest": "^4.1.11"
    }
}
